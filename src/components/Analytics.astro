---
const websiteId = import.meta.env.PUBLIC_UMAMI_WEBSITE_ID;
const scriptUrl = import.meta.env.PUBLIC_UMAMI_SCRIPT_URL;
const isEnabled = import.meta.env.PROD && websiteId && scriptUrl;
---

{
  isEnabled && <script defer src={scriptUrl} data-website-id={websiteId}></script>
}
