---
import { YouTube as YouTubeEmbed } from "@astro-community/astro-embed-youtube";

interface Props {
  videoId: string;
}

const { videoId } = Astro.props;
---

<div class="relative w-full h-0 pb-[56.25%] my-6">
  <YouTubeEmbed id={videoId} class="absolute top-0 left-0 w-full h-full" />
</div>
